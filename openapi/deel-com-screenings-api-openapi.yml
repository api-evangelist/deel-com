openapi: 3.2.0
info:
  title: Deel Platform Extensions Screenings API
  description: 'Specialized platform extensions covering background screenings (KYC/AML), immigration

    case management (visa types, business visa eligibility, work permits, right-to-work

    cases, document upload), and Deel IT (device provisioning, equipment assets, asset

    orders) for distributed workforce hardware management.'
  version: '2026-05-25'
  contact:
    name: Deel Developer Support
    url: https://developer.deel.com/api/platform/introduction
servers:
- url: https://api.letsdeel.com/rest/v2
  description: Production
- url: https://api-sandbox.demo.deel.com/rest/v2
  description: Sandbox
security:
- BearerAuth: []
tags:
- name: Screenings
  description: KYC and AML background screenings
paths:
  /screenings:
    get:
      operationId: getScreenings
      summary: List Background Screenings
      tags:
      - Screenings
      parameters:
      - name: person_id
        in: query
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - in_progress
          - completed
          - failed
          - cancelled
      responses:
        '200':
          description: Screenings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Screening'
    post:
      operationId: createScreening
      summary: Create Background Screening
      tags:
      - Screenings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScreeningCreate'
      responses:
        '201':
          description: Screening created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Screening'
  /screenings/{screening_id}:
    get:
      operationId: getScreening
      summary: Retrieve A Screening
      tags:
      - Screenings
      parameters:
      - name: screening_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Screening detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Screening'
components:
  schemas:
    Screening:
      type: object
      properties:
        id:
          type: string
        person_id:
          type: string
        type:
          type: string
          enum:
          - identity
          - criminal
          - credit
          - employment_history
          - education
          - sanctions
          - aml
          - kyc
        status:
          type: string
          enum:
          - pending
          - in_progress
          - completed
          - failed
          - cancelled
        result:
          type: string
          enum:
          - clear
          - consider
          - suspected
          - withdrawn
        country:
          type: string
        created_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        report_url:
          type: string
          format: uri
    ScreeningCreate:
      type: object
      required:
      - person_id
      - type
      - country
      properties:
        person_id:
          type: string
        type:
          type: string
          enum:
          - identity
          - criminal
          - credit
          - employment_history
          - education
          - sanctions
          - aml
          - kyc
        country:
          type: string
        consent_obtained:
          type: boolean
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: opaque
